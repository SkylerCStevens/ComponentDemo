import React from 'react';

export type InputProps = {
  label: string;
  placeholder: string;
  visibleLabel: boolean;
  fontColor: string;
  fontSize: string;
  outlineColor: string;
};

export function Input({ 
  label,
  placeholder,
  visibleLabel,
  fontColor,
  fontSize,
  outlineColor
 }: InputProps) {
  return (
    <div>
      {}
    </div>
  );
}
